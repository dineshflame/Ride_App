<?xml version="1.0" encoding="utf-8"?>


<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/fragment_container_car"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:id="@+id/carSelectInstruction"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/selectCar"
            android:textAllCaps="false"
            android:textSize="18sp"
            android:textStyle="bold"/>

    <ExpandableListView
        android:id="@+id/expandableListView1"
        android:layout_width="384dp"
        android:layout_height="match_parent"
        android:layout_gravity="bottom"
        android:childDivider="@color/colorPrimaryDark"
        android:childIndicatorEnd="@dimen/activity_horizontal_margin"
        android:clipChildren="true"
        android:contentDescription="@string/app_name"
        android:footerDividersEnabled="true"
        android:headerDividersEnabled="true">


    </ExpandableListView>
    </LinearLayout>
</FrameLayout>
